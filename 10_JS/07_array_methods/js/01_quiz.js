      // 퀴즈: 아래를 배열메소드로 변경해보세요: forEach() 사용
      let a = ["a", "b", "c"];
      for (let i = 0; i < a.length; i++) {
        console.log(a[i]);
      }
    //   풀이:
    a.forEach((v)=>console.log(v));